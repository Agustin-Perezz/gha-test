name: test github actions

on:
    push:
        branches:
            - main

jobs:
    hello_word:
        runs-on: ubuntu-latest
        steps:
            - name: echo The message
              run: echo "Hola pedordo!"
